# План проекта Skillaz Start

Статическая интерактивная страница для продуктового плана по релизам 09.09, 07.10 и 05.11. Источники собраны из Yandex Tracker, проектного решения Skillaz AI и роадмапа внутренней витрины вакансий.

## Обновление данных

1. Открыть основную страницу и нажать `Администрирование`.
2. Ввести GitHub-токен с правом `Contents: Read and write` только для репозитория `Yulia405/skillaz-product-roadmap`.
3. Для фичи открыть карточку и изменить статус, готовность или комментарий.
4. Для промо включить администрирование и нажать кнопку с карандашом на нужной карточке.
5. Нажать `Сохранить для всех`. Изменения записываются в `roadmap-overrides.json` и после публикации доступны всем посетителям.

Токен хранится только в памяти текущей вкладки и не записывается в код, `localStorage` или ссылку. Коллеги могут редактировать план, если им выдан доступ на запись в репозиторий и они используют собственный токен.

Кнопка `Поделиться` формирует актуальную ссылку с параметром `?view=1`, в которой скрыты инструменты администрирования, либо зафиксированный снимок для отчёта.

Статусы также сверяются автоматически по понедельникам, вторникам и средам в 10:00 по Москве. При изменениях обновляется `roadmap-data.js`, выполняется проверка страницы и публикуется новая версия GitHub Pages.

## Данные

Базовый состав фич находится в `roadmap-data.js`. Статусы и проценты на старте являются продуктовой оценкой по состоянию источников на 09.09.2026 и обновляются владельцем плана.

## Локальный запуск

```powershell
node server.js
```

Страница доступна по адресу `http://127.0.0.1:8770/`.
